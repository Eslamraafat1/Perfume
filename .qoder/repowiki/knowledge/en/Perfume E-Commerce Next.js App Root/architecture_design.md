Flat project root with no source subdirectories visible in scope — only configuration manifests that bootstrap the app: `next.config.ts` whitelists `*.supabase.co` for Next Image remote patterns; `tsconfig.json` enables strict mode, bundler module resolution, React JSX transform, and the `@/*` path alias pointing at the repo root; `eslint.config.mjs` composes `eslint-config-next/core-web-vitals` and `typescript` presets while re-enabling `.next`, `out`, `build`, and `next-env.d.ts` via `globalIgnores`; `postcss.config.mjs` + `tailwindcss` v4 drive styling; `supabase-setup.sql` provisions the database schema used by `@supabase/supabase-js` client code elsewhere in the tree. Dependency direction is outward only — this leaf declares runtime (`next`, `react`, `gsap`, `@supabase/supabase-js`) and dev tooling but contains no application logic itself.